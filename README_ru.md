# Togglr

[![CI](https://github.com/togglr-project/togglr/actions/workflows/ci.yml/badge.svg)](https://github.com/togglr-project/togglr/actions/workflows/ci.yml)

Togglr — это **платформа для управления feature flags и экспериментов** для разработчиков и продуктовых команд.

Она позволяет **включать и выключать функции**, проводить **A/B и многовариантные эксперименты**, а также постепенно раскатывать функции на основе сегментов пользователей, времени или адаптивных алгоритмов обучения.

## Содержание

- [🚀 Эксперименты и алгоритмы](#-эксперименты-и-алгоритмы)
  - [Поддерживаемые алгоритмы](#поддерживаемые-алгоритмы)
  - [Архитектура экспериментов](#архитектура-экспериментов)
- [Возможности](#возможности)
- [Архитектура](#архитектура)
- [Установка](#установка)
  - [Системные требования](#системные-требования)
  - [Быстрая установка](#быстрая-установка)
  - [Ручная установка](#ручная-установка)
  - [Переменные окружения](#переменные-окружения)
  - [После установки](#после-установки)
- [Настройка dev-окружения](#настройка-dev-окружения)
- [Использование](#использование)
- [Участие в разработке](#участие-в-разработке)
- [Решение проблем](#решение-проблем)
  - [Проблемы с установкой](#проблемы-с-установкой)
  - [Управление платформой](#управление-платформой)
  - [Получение помощи](#получение-помощи)

---

## 🚀 Эксперименты и алгоритмы

Togglr расширяет классические feature flags до **полноценной платформы экспериментов**.

Каждая функция может иметь несколько **вариантов** (A/B/N группы), и вы можете прикрепить **алгоритм**, который динамически контролирует, какой вариант показывать пользователям — автоматически оптимизируя для вознаграждения или конверсии.

### Поддерживаемые алгоритмы

В настоящее время реализованы:

- **ε-Greedy** — исследует случайно с вероятностью *ε*, в остальных случаях использует лучший вариант.
- **Thompson Sampling** — байесовский подход, балансирующий исследование и использование.
- **Upper Confidence Bound (UCB)** — выбирает вариант с наивысшей границей доверия для ожидаемого вознаграждения.

Каждый алгоритм может быть настроен для каждого окружения (например, `production`, `staging`) с пользовательскими числовыми настройками (например):

```json
{
  "epsilon": 0.1
}
```

### Архитектура экспериментов

* Каждая **функция** имеет один активный алгоритм на **окружение**.
* Каждый алгоритм отслеживает статистику (оценки, успехи, неудачи, вознаграждения) для каждого варианта.
* События обратной связи (от SDK или API) агрегируются в TimescaleDB.
* **BanditManager** хранит живую статистику в памяти и периодически синхронизирует её с TimescaleDB.
* Алгоритмы учатся со временем и динамически корректируют раскатку — ручная настройка не требуется.

> "Один алгоритм — один мозг на функцию.
> Варианты — это его руки.
> Обратная связь — это его опыт."

---

## Возможности

* Feature flags, организованные по проектам и окружениям (prod, stage, dev)
* Многовариантные флаги и эксперименты раскатки
* Алгоритмы для адаптивной раскатки (ε-Greedy, Thompson Sampling, UCB)
* Защищенные функции (ожидающие изменения с рабочим процессом одобрения)
* Категории и теги для организации функций
* Сегменты и правила таргетинга для контекста пользователя
* Расписания для автоматической активации/деактивации
* Полный аудит-лог изменений
* Мониторинг здоровья функций
* Автоматическое отключение при ошибках выполнения (через отчеты об ошибках)
* Контроль доступа на основе ролей (RBAC)
* REST API и события WebSocket
* SDK для нескольких языков
* 2FA, LDAP, SSO/SAML аутентификация

---

## Архитектура

* **Backend** — Go (TimescaleDB, NATS JetStream, REST API, WebSocket broadcaster)
* **Frontend** — React + TypeScript
* **SDK** — Go, Python, Ruby, TypeScript, Elixir, Rust

Данные экспериментов и обратная связь (непрерывные агрегаты для дашбордов), конфигурация алгоритмов и состояние хранятся в TimescaleDB.

---

## Установка

### Системные требования

**Минимальные требования:**
- Linux или macOS
- Docker 20.10+
- Docker Compose 2.0+
- 2GB RAM
- 10GB свободного места на диске
- Доступ root/sudo

**Рекомендуется для production:**
- 4GB+ RAM
- 50GB+ свободного места на диске
- SSL сертификат (рекомендуется Let's Encrypt)
- SMTP сервер для уведомлений по email

### Быстрая установка

Самый простой способ установить Togglr — использовать наш автоматизированный скрипт установки:

```bash
# Скачать и запустить быстрый установщик
curl -fsSL https://raw.githubusercontent.com/togglr-project/togglr/main/prod/install.sh | sudo bash
```

Или если у вас есть локальная копия репозитория:

```bash
cd togglr
sudo ./prod/quick_install.sh
```

Установщик:
- Проверит системные требования (Docker, Docker Compose, OpenSSL, Make)
- Проведет вас через процесс конфигурации
- Создаст необходимые директории и файлы конфигурации
- Сгенерирует SSL сертификаты (самоподписные или использует ваши существующие)
- Настроит полную платформу Togglr

### Ручная установка

Если вы предпочитаете установку вручную или нужна кастомная конфигурация:

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/togglr-project/togglr.git
   cd togglr
   ```

2. **Запустите установщик:**
   ```bash
   sudo ./prod/quick_install.sh
   ```

3. **Следуйте интерактивным подсказкам** для настройки:
   - Email администратора
   - Имя домена
   - Настройки SSL сертификата
   - Конфигурация SMTP сервера

### Переменные окружения

Для неинтерактивной установки вы можете установить переменные окружения:

```bash
sudo TOGGLR_ADMIN_EMAIL=admin@example.com \
     TOGGLR_DOMAIN=example.com \
     TOGGLR_MAILER_ADDR=smtp.example.com:587 \
     TOGGLR_MAILER_USER=admin@example.com \
     TOGGLR_MAILER_PASSWORD=your_password \
     TOGGLR_MAILER_FROM=noreply@example.com \
     TOGGLR_HAS_SSL_CERT=false \
     ./prod/install.sh
```

**Доступные переменные окружения:**
- `TOGGLR_ADMIN_EMAIL` - Email администратора (обязательно)
- `TOGGLR_DOMAIN` - Домен платформы (обязательно)
- `TOGGLR_MAILER_ADDR` - Адрес SMTP сервера (обязательно)
- `TOGGLR_MAILER_USER` - Пользователь SMTP (по умолчанию: admin@domain)
- `TOGGLR_MAILER_PASSWORD` - Пароль SMTP (по умолчанию: password)
- `TOGGLR_MAILER_FROM` - Адрес отправителя email (по умолчанию: noreply@domain)
- `TOGGLR_HAS_SSL_CERT` - Есть ли у вас существующий SSL сертификат (по умолчанию: false)

### После установки

После установки платформа будет доступна по адресам:
- **Frontend**: `https://your-domain`
- **API**: `https://your-domain/api/v1/`
- **SDK**: `https://your-domain/sdk/v1/`

**Учетные данные по умолчанию:**
- Email: Email администратора, который вы указали при установке
- Пароль: Временный пароль, сгенерированный при установке (при первом входе потребуется изменить)

**Управление платформой:**
```bash
cd /opt/togglr
make up    # Запустить платформу
make down  # Остановить платформу
make pull  # Обновить Docker образы
```

---

## Настройка dev-окружения

### Требования

* Docker
* Docker Compose

### Быстрый старт

```bash
git clone https://github.com/togglr-project/togglr.git
cd togglr
make setup
make dev-up
```

Откройте:

* Frontend: [https://togglr.local](https://togglr.local)
* API: [https://togglr.local/api/v1/](https://togglr.local/api/v1/)
* SDK: [https://togglr.local/sdk/v1/](https://togglr.local/sdk/v1/)

### Примечания по конфигурации

* Домен по умолчанию: `togglr.local`
* SSL: самоподписные сертификаты в `dev/nginx/ssl/`
* Суперпользователь по умолчанию:

    * Email: `admin@togglr.dev`
    * Пароль: `password543210`

---

## Использование

* REST API под `/api/v1/*`
* События WebSocket под `/api/ws`
* SDK endpoints под `/sdk/v1/*`
* Метрики Prometheus на `:8081/metrics`

---

## Участие в разработке

Мы приветствуем вклад сообщества в Togglr!

Все вклады подчиняются [Togglr Business License (TBL)](./LICENSE)
и нашему [Contributor License Agreement (CLA)](./CLA.md).

### Как внести вклад

1. Форкните репозиторий.
2. Создайте ветку для функции.
3. Внесите ваши изменения.
4. Отправьте pull request.

Пожалуйста, убедитесь, что включили тесты и обновления документации, где это уместно.

---

## Решение проблем

### Проблемы с установкой

**Установщик зависает или замораживается:**
- Убедитесь, что вы запускаете в интерактивном терминале
- Проверьте, что у вас есть права sudo/root
- Попробуйте запустить с переменными окружения для неинтерактивного режима

**Проблемы с правами Docker:**
```bash
sudo usermod -aG docker $USER
# Выйдите и войдите снова
```

**Проблемы с SSL сертификатом:**
- Для production используйте Let's Encrypt или сертификат от доверенного CA
- Поместите файлы сертификата в `/opt/togglr/nginx/ssl/nginx_cert.pem` и `/opt/togglr/nginx/ssl/nginx_key.pem`

**Проблемы с подключением к базе данных:**
- Проверьте, запущен ли контейнер PostgreSQL: `docker ps`
- Проверьте учетные данные базы данных в `/opt/togglr/config.env`
- Проверьте логи: `docker logs togglr-postgresql`

### Управление платформой

**Запуск/остановка платформы:**
```bash
cd /opt/togglr
make up    # Запустить все сервисы
make down  # Остановить все сервисы
```

**Просмотр логов:**
```bash
docker logs togglr-backend
docker logs togglr-frontend
docker logs togglr-postgresql
```

**Обновление платформы:**
```bash
cd /opt/togglr
make pull  # Скачать последние образы
make up    # Перезапустить с новыми образами
```

**Сброс пароля администратора:**
```bash
# Отредактируйте /opt/togglr/config.env и измените ADMIN_TMP_PASSWORD
# Затем перезапустите: make down && make up
```

### Получение помощи

- **Документация**: Проверьте этот README и встроенную справку
- **Issues**: Сообщайте об ошибках в [GitHub Issues](https://github.com/togglr-project/togglr/issues)
- **Discussions**: Присоединяйтесь к обсуждениям сообщества в [GitHub Discussions](https://github.com/togglr-project/togglr/discussions)