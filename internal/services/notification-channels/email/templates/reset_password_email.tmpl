<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            font-size: 14px;
            background-color: #1c1e23;
            color: #f5f5fa;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            margin: 24px auto;
            padding: 24px;
            background: linear-gradient(to bottom, #2d3038, #23262e);
            border: 1px solid #3a3d4a;
            border-radius: 12px;
            box-shadow: 0 8px 24px 0 rgba(0, 0, 0, 0.3);
            text-align: center;
        }
        .header {
            padding-bottom: 16px;
            border-bottom: 1px solid #3a3d4a;
        }
        .title {
            font-size: 22px;
            font-weight: 600;
            color: #f5f5fa;
        }
        .content {
            padding: 24px 0;
            font-size: 15px;
            line-height: 1.6;
        }
        .button {
            display: inline-block;
            padding: 12px 24px;
            font-size: 15px;
            color: #ffffff;
            background: linear-gradient(45deg, #8252ff, #966eff);
            border-radius: 10px;
            text-decoration: none;
            font-weight: 500;
            margin-top: 16px;
            box-shadow: 0 2px 8px 0 rgba(0, 0, 0, 0.2);
            transition: all 0.2s ease-in-out;
        }
        .button:hover {
            background: linear-gradient(45deg, #8a61ff, #a07eff);
            box-shadow: 0 4px 12px 0 rgba(0, 0, 0, 0.3);
            transform: translateY(-1px);
        }
        .footer {
            margin-top: 24px;
            font-size: 13px;
            color: #8892a0;
            text-align: center;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="header">
        <div class="title">Togglr Password Reset</div>
    </div>
    <div class="content">
        <p>Hello,</p>
        <p>We received a request to reset the password for your Togglr account. Click the button below to proceed.</p>
        <a href="{{ .ResetLink }}" class="button">Reset Password</a>
    </div>
    <div class="footer">
        If you did not request a password reset, please ignore this email.<br />— The Togglr Team
    </div>
</div>
</body>
</html> 